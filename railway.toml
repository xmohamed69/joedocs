[build]
builder = "dockerfile"

[deploy]
startCommand = "python manage.py migrate --noinput && gunicorn joedocs.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
healthcheckPath = "/website/"
restartPolicyType = "on_failure"